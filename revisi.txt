navbar dibuat lebih interaktif dan beranimasi, misalnya garis underline dibuat hanya muncul pas di hover

panah di ongoing event dibuat lebih kecil

display introduction layoutnya ngikutin mobile

contact us dihapus, ubah jdi section buku tamu untuk pengganti contact email, 
sedangkan whatsapp diubah jdi bubble yang hover di pojok kanan layar


backup:


<li>
        <button
          type="button"
          onClick={() => handleScroll("hero")}
          className={`text-gray-300 font-extrabold text-[clamp(0.9rem,1.1vw,5rem)] border-amber-500 hover:text-gray-400 transition-colors duration-200
            ${currSect === "hero" ? "border-t-4" : "border-t-4"}  
          `}
        >
          HOME
        </button>
      </li>
      <li>
        <button
          type="button"
          onClick={() => handleScroll("event")}
          className="text-gray-300 font-extrabold text-[clamp(0.9rem,1.1vw,5rem)] hover:text-gray-400 transition-colors duration-200"
        >
          Lihat Event
        </button>
      </li>
      <li>
        <button
          type="button"
          onClick={() => handleScroll("about")}
          className="text-gray-300 font-extrabold text-[clamp(0.9rem,1.1vw,5rem)] hover:text-gray-400 transition-colors duration-200"
        >
          About Us
        </button>
      </li>
      <li>
        <button
          type="button"
          onClick={() => handleScroll("faq")}
          className="text-gray-300 font-extrabold text-[clamp(0.9rem,1.1vw,5rem)] hover:text-gray-400 transition-colors duration-200"
        >
          FAQ
        </button>
      </li>
      <li>
        <button
          type="button"
          onClick={() => {
            alert("coming soon");
            if (onNavigate) onNavigate();
          }}
          className="text-gray-300 font-extrabold text-[clamp(0.9rem,1.1vw,5rem)] hover:text-gray-400 transition-colors duration-200"
        >
          Daftarkan Event
        </button>
      </li>